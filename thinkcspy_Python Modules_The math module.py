import math

print(math.pi)
print(math.e)

print(math.sqrt(2.0))

print(math.sin(math.radians(90)))   # sin of 90 degrees

# Mathematical functions do not need to be constructed. They simply perform a task. 


import turtle

alex=turtle.Turtle()     
alex.shape("turtle")

# a turtle is a data object (recall alex). We need to create one in order to use it. When we say alex = turtle.Turtle(), we are calling the constructor for the Turtle class which returns a single turtle object.
